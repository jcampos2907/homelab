---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  namespace: onvo-mcp
  name: vault-auth
spec:
  kubernetes:
    role: onvo-mcp
    audiences:
      - https://kubernetes.default.svc
  vaultAuthGlobalRef:
    namespace: hashicorp
    allowDefault: true
