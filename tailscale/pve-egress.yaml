    apiVersion: v1
    kind: Service
    metadata:
      annotations:
        tailscale.com/tailnet-ip: "100.110.67.76"
        tailscale.com/proxy-group: "egress-proxies"
      name: pve-egress
      namespace: default
    spec:
      externalName: placeholder # any value - will be overwritten by the operator
      type: ExternalName
      ports:
      - port: 8080
        protocol: TCP
        name: web # any value
      - port: 7480
        protocol: TCP
        name: s3 # any value
      - port: 8443
        protocol: TCP
        name: cephui # any value
