---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  namespace: oauth2
  name: vault-oauth2-secrets-static
spec:
  vaultAuthRef: vault-auth
  rolloutRestartTargets:
    - kind: Deployment
      name: oauth2-oauth2-proxy
  mount: kv
  type: kv-v2
  path: oauth2/proxysecrets
  refreshAfter: 60s
  destination:
    create: true
    name: oauth2-secrets

    
