---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  namespace: oauth2
  name: vault-auth
spec:
  vaultAuthGlobalRef:
    namespace: hashicorp
    allowDefault: true
  kubernetes:
    audiences:
      - https://kubernetes.default.svc
    role: oauth2
