---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  namespace: cloudflare
  name: vault-auth
spec:
  kubernetes:
    role: cloudflare
    audiences:
      - https://kubernetes.default.svc
  vaultAuthGlobalRef:
    namespace: hashicorp
    allowDefault: true
